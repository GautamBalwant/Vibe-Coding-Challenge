
# Prompt: Update README for Setup & Run

**Goal**: Provide clear setup instructions.

**Instructions**
- Prereqs: Node.js, Java 17+, Maven.
- Steps: install UI deps, run backend, configure `.env` & `application.yml`, start both servers.
- Include demo admin credentials and sample data.

**Acceptance Criteria**
- New contributors can run in ≤10 minutes.
